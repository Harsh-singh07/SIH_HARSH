async function adminGuard(){return guard('admin')}async function createStaff(e){e.preventDefault();const f=Object.fromEntries(new FormData(e.target));const{data,error}=await client().functions.invoke('create-staff',{body:f});const o=document.querySelector('#result');o.className='notice'+(error?' error':'');o.textContent=error?.message||`Operator created. One-time temporary password: ${data.temporary_password}`}
